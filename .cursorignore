# Cursor indexing scope — tracked in git (paired with .gitignore)
# Python / local env
.venv/*
**/.venv/**
**/venv/**

# Google Antigravity / multi-agent tool dirs (avoid cross-tool index conflicts; paths may evolve)
**/.agent/**
**/.gemini/**
.geminiignore
