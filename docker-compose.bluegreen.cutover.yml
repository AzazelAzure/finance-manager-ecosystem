# Legacy: Postgres reuse was merged here for `docker compose -f ... -f docker-compose.bluegreen.cutover.yml`.
# `docker-compose.bluegreen.yml` now declares `postgres_data` as external `finance_manager_postgres_data`
# directly, because podman-compose 1.0.x cannot merge a bare `postgres_data:` key with a mapping
# (ValueError: can't merge ... NoneType and dict).
#
# Use only:
#   podman compose -f docker-compose.bluegreen.yml --env-file .secrets/server.env up -d
#
# Greenfield: create the volume once — `podman volume create finance_manager_postgres_data`.
services: {}
