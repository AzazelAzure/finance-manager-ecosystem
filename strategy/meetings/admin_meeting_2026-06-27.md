# Admin Morning Meeting — 2026-06-27

**Start:** morning  
**Format:** live follow-along doc — check items off as covered

---

## AGENDA

- [ ] 1. Daily summary review + current_status.md living doc decision
- [ ] 2. API status — what shipped, what's still behind on VPS
- [ ] 3. Track 1 — Cursor audit (JWT / Dexie / F-013 retention)
- [ ] 4. Open PRs
- [ ] 5. Park / carry-forward

---

## 1. Daily Summary Review

**Source:** `strategy/daily_summaries/daily_summary_2026-06-27.md`

### What shipped since last current_status.md (current_status.md is stale — missing all of this)


| Area                    | What landed                                                                                                                                                                                                                                                           |
| ----------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **F-014**               | Full backend: `DailyUsageSnapshot`, `InviteChainEvent`, `OperatorAlertState` models; migration `0010_usage_monitoring_f014`; async `notify.py` Celery task; bug report email now async                                                                                |
| **Celery**              | Autodiscovery bug fixed (`tasks/__init__.py` created); Celery wired into blue-green deploy (`docker-compose.bluegreen.yml` + `fm_server_beta.sh`); orphan pruning added                                                                                               |
| **Security fix**        | XSS vulnerability patched in weekly digest (`support_digest.py`) — user-submitted fields now HTML-escaped before email interpolation                                                                                                                                  |
| **UI/UX design system** | `tokens.css` (OKLCH semantic colors, 4px scale, dark-mode flash protection, tabular numerics); `Input.tsx`; `ProtectedShell.tsx`; `shells.css` (mobile bottom nav + slide-up drawer, z-index contract); Framer Motion (reduced-motion gated); i18n: `en-US` + `tl-PH` |
| **SEO**                 | `LoginPage.tsx` + `SignupPage.tsx` — localized document meta via React Helmet                                                                                                                                                                                         |
| **Deploy hardening**    | Compose output redaction (PR #68); container orphan pruning (`prune-orphans` command); `smtp.secret` excluded from git                                                                                                                                                |


### Watchlist from summary (carry into VPS sync planning)

1. **Celery not running on VPS** — worker + beat containers not in Podman stack yet; F-014 alerts and weekly digest are silent until deployed ⚠️ [Comment: This is untrue.  This is verified working, however, there are parts that are not verify functionality, namely feature requests.  We will also need to wire in things for support and privacy emails, and potential email replies for UX to note a bug report HAS been made and seen besides just a notification.  That said, we will need to add security to ensure no spam is made here, and emails are sent to a user input email, not pulled from server.  Worth looking into for later]
2. **VPS submodule sync pending** — target: API `789b266`, Web `4ad032a`; needs blue rebuild + migrations `0010` + `axes` + smoke before promote [Comment: I will send an agent to confirm Ecosystem repo is correct and a full sync to main, along with merging the doc signup.  We may need to pull certain files out of the ecosystem and into .gitignore so we don't need a PR for these, but that's a pro/con decision on paper trail vs conveniece)
3. **Web build at `9b2ecbe` was broken** — confirm `4ad032a` builds clean before VPS pull [Comment: current live version is functioning by test, will have to send this to a background task during the meeting for validation.]

### Living doc decision — current_status.md

**Problem:** `current_status.md` is generated by Antigravity but the daily summaries have moved faster — current_status is already missing F-014, the design system, Celery deploy wiring, and three merged PRs.

**Options:**

- [x] **A — Keep current_status.md as a living index** — Antigravity automation reads all daily summaries and regenerates current_status.md weekly (or on-demand), with links to individual daily reports for drill-down. Best of both worlds.[Comment: This will require a rework of the automation prompt, can provide this later, since I think adding a list of PRs and status for them in the daily summary will be useful as well.]
- [ ] **B — Retire current_status.md** — Daily summaries are the source of truth; morning meetings read the latest summary directly
- [ ] **C — Keep both as-is** — current_status.md stays as a periodic deep snapshot; daily summaries handle the delta

> **Recommendation:** Option A. Current_status.md as an auto-generated index with links is more useful than either file alone — gives a single-pane view with drill-down available. Antigravity already generates the daily summaries; adding a rollup pass is low lift.

**Decision:** _______________

---

## 2. API Status — What's Behind on VPS

### Current VPS state (green active)


| Submodule | VPS                                           | Target    | Gap                                                                                              |
| --------- | --------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------ |
| API       | old (`1833e74` — May 4) via standby SHA chain | `789b266` | F-013 Loguru, F-014 models/tasks/migrations, API security WIP, Celery autodiscovery fix, XSS fix |
| Web       | `4ad032a` (standby target — pinned post #64)  | `4ad032a` | Confirmed clean if build validates                                                               |


### What needs to happen before VPS is current

1. **Merge PR #65** (standby smoke pass docs) — open right now, blocking branch cleanup
2. **Blue rebuild on VPS** — pull API `789b266` + Web `4ad032a`, rebuild inactive blue
3. **Run migrations on blue** — `0010_usage_monitoring_f014` + `migrate axes`
4. **Wire Celery on VPS** — worker + beat containers need to be added to Podman stack + SMTP env vars set
5. **Smoke blue** → promote blue → green becomes inactive

### API security hardening WIP (still local, uncommitted)

From current_status.md §8 — these local changes are not in any PR yet:

- `django-axes` config (5 failures → 1hr lockout)
- Argon2 default hasher
- Min password 12 + `ComplexPasswordValidator`
- HSTS, nosniff, X-Frame-Options

**Blocker:** validator file untracked, no `migrate axes` yet, no proxy IP config for Cloudflare, detached HEAD, no tests.  
**Question:** Is the API security hardening sprint still in queue, or was it absorbed into what Cursor shipped? Check `plans/S1/S1.B/feat-api-security-hardening/` for current task status.

---

## 3. Track 1 — Cursor Audit (Today's Primary Execution Target)

**Goal:** Resolve three unknowns before any legal document can be published.


| Gap                  | Question                                                                                                  | Why it matters                                                          |
| -------------------- | --------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| #1 — JWT storage     | Where are access + refresh tokens stored in `finance_manager_web`? localStorage? Memory? HttpOnly cookie? | Cookie consent category classification; security posture                |
| #5 — Dexie key       | How is the Dexie.js encryption key stored/derived? Is it in localStorage?                                 | Privacy policy can't claim "encryption-at-rest" until this is confirmed |
| N6 — F-013 retention | What is the Loguru log retention/rotation config on the API side? How long do per-UUID files live?        | §3.8 data retention table can't be published without this               |


### Suggested cagent prompt (fire when ready)

```
Audit three things and report findings per item:

1. JWT STORAGE — In finance_manager_web, where are JWT tokens stored? Specifically: 
   (a) access token — is it in memory (JS variable), localStorage, sessionStorage, or a cookie?
   (b) refresh token — same question. Check the auth service, token interceptor, and any useAuth/useTokens hooks.

2. DEXIE ENCRYPTION KEY — In finance_manager_web, how is the Dexie.js encryption key obtained?
   Is it derived from a user secret via KDF, fetched from the API backend after auth, stored in localStorage, or stored in sessionStorage?
   Find the Dexie database initialization code and trace the key source.

3. F-013 LOGURU RETENTION — In finance_manager_api, what is the Loguru configuration for per-user/per-UUID log files?
   Specifically: is there a rotation policy (size or time-based), a retention period, and a compression setting?
   Find where Loguru is configured for the F-013 diagnostic log files specifically (not the general app log).

Report each finding with the file path and relevant line numbers.
```

### Expected outcomes and their impact


| Finding                                   | If YES                                                  | If NO / different                                          |
| ----------------------------------------- | ------------------------------------------------------- | ---------------------------------------------------------- |
| Refresh token in HttpOnly cookie          | Cookie policy: strictly necessary (exempt from consent) | Must add cookie migration to API security hardening sprint |
| Access token in memory (not localStorage) | 2026 gold standard — state this in security measures    | Flag as technical debt; add to sprint                      |
| Dexie key NOT in localStorage             | Privacy policy §3.12 can claim encryption-at-rest       | Cannot claim until fixed; add to sprint                    |
| F-013 has defined retention               | §3.8 table is completable                               | Add "retention TBD" note; add Cursor task to set retention |


### After audit results come back

- Update §1.4 of `cookie_consent_requirements.md` with confirmed JWT method
- Update §3.12 of `privacy_policy_requirements.md` with confirmed Dexie key method
- Fill F-013 row in `§3.8` data retention table
- If JWT is still in localStorage → add migration task to `feat-api-security-hardening` plan scope

---

## 4. Open PRs


| PR  | Title                                                   | Status | Action                                         |
| --- | ------------------------------------------------------- | ------ | ---------------------------------------------- |
| #65 | docs(strategy): record inactive blue standby smoke pass | OPEN   | Merge — clean docs PR, unblocks branch cleanup |


All others (#66, #67, #68) merged yesterday. No stale daily-status relics remaining.

---

## 5. Park / Carry-Forward


| Item                                         | Status                                               | Next trigger                          |
| -------------------------------------------- | ---------------------------------------------------- | ------------------------------------- |
| Quarterly self-review (kill_commit_gates §6) | **Due tomorrow 2026-06-28**                          | Schedule first thing tomorrow morning |
| VPS sync + blue rebuild                      | Parked — waiting on PR #65 merge + audit results     | After today's agenda                  |
| API security hardening WIP                   | Parked — local only, not commit-ready                | After security plan status check      |
| Legal document publication                   | Parked — waiting on Track 1 audit results + attorney | After audit                           |
| Track 2 (governance artifacts)               | N2 clickwrap spec, N4 Dexie opt-out spec             | This session if time permits          |
| Track 3 (N1 financial advice scope)          | HitM to describe scope of literacy content           | This session if time permits          |
| Entity formation                             | S1.B exit gate — no action yet                       | Whenever attorney contact happens     |
| Android pull-forward                         | Not started — `android:Scaffold`                     | S1.B work queue                       |


---

## Notes / Decisions Made This Meeting

- [x] **current_status.md living doc decision: Option A** — Antigravity regenerates current_status.md from daily summaries (rollup pass); automation prompt needs rework; add PR list + status to daily summary format. Daily summaries directory is `strategy/daily_summaries/` (note: not `strategy/daily_summary`).
- [x] **Celery watchlist item 1 was wrong** — Celery IS verified working on VPS. Actual gaps: feature request email unverified; user-facing confirmation email (bug report received) doesn't exist; support@ and privacy@ not yet wired.
- [x] **Track 1 audit complete** — JWT: both tokens in localStorage (security issue). Dexie: no encryption. F-013: 14-day retention, 10MB rotation, probabilistic cleanup. Bonus: usernames logged in plain text in general Loguru sinks (signals.py).
- [x] **T05 and T06 created** in `feat-api-security-hardening` — JWT HttpOnly cookie migration + Dexie encryption. Existing T03/T04 are migrations/changelog (different scope).
- [x] **Legal workflow coordination doc created** at `strategy/legal/legal_workflow_coordination.md` — handoff doc for Claude Web; defines roles, draft directory, pending markers, cagent query format.
- [x] **F-014 invite/share** — link-based approach is cleanest (aligns with privacy goals); substantial workload (API tracking, clickthrough to signup, Cloudflare tunnel, traffic attribution). Worth it long-term. Parked as future discussion item — not current sprint.
- [x] **CI pipeline** — noted as future discussion item; will require focused effort alongside UI/UX research. Visual regression testing + build validation + Playwright smoke as minimum viable. Parked for S1.C prep discussion.
- [x] **Admin artifacts restructure** — current huddle/meeting docs becoming unwieldy; need condensed decision matrix system; must maintain flexibility (payment providers shouldn't gate unrelated systems). Governance task for future session.
- [x] **cagent role clarified** — information gathering only in this project; no coding. Cursor/IDE agents handle all implementation. Claude Web handles legal document drafting.

---

## Post-Meeting Corrections (2026-06-27 — second session)

- [x] **Sessions 4 and 6 CLOSED.md files** — written (directories created + CLOSED.md files at `strategy/huddles/admin-meeting-huddle-prep-2026-05-06/sessions/session-04-infra-security/` and `session-06-strategy-wrapup/`)
- [x] **cagent output recovered** — output from background file captured; all three verified
- [x] **Argon2 confirmed live** — `settings.py:334-340`; PBKDF2 is verify-only fallback for legacy hashes; T01/T02 `[PENDING]` marker removed from `privacy_policy_v1.md §9`
- [x] **Dexie 3-month auto-purge does NOT exist** — `seed.ts:31-48` is a seed window (writes), not a purge; no date-sweep deletion found anywhere; all three policy drafts corrected; "3-month auto-purge" language removed
- [x] **F-013 mtime confirmed** — 14-day, probabilistic (~1%), mtime-based; already correctly reflected in docs
- [x] **US market position documented** — added to `legal_workflow_coordination.md`; US = CYA baseline (same as EU), not shutdown, not primary market; CCPA/CalOPPA sections unchanged
- [x] **Honorary Founders redefinition** — concept retained; audience shifts from US friends → PH beta testers; definition of what "Honorary Founder" means in PH context is open for future discussion
- [x] **Legal workflow coordination doc updated** — cagent verification results table added; US market position section added; draft status table updated to reflect all three v1 drafts now exist

## Action Items Coming Out of This Meeting

| # | Item | Owner | By when |
|---|---|---|---|
| 1 | Quarterly self-review — `kill_commit_gates.md` §6, three written questions | HitM | Tomorrow 2026-06-28 |
| 2 | Merge PR #65 (standby smoke pass docs) | HitM / Cursor | Today |
| 3 | Cursor execution: T05 JWT HttpOnly cookie migration | Cursor (IDE) | Next security sprint |
| 4 | Cursor execution: T06 Dexie encryption | Cursor (IDE) | Next security sprint |
| 5 | Claude Web: draft policy text using `legal_workflow_coordination.md` as context; drop to `strategy/legal/drafts/` | Claude Web | When HitM initiates |
| 6 | cagent: verify Dexie 3-month auto-purge job exists and where | cagent | Before privacy policy finalizes |
| 7 | Antigravity: rework daily summary automation to include PR list + status; add rollup pass to regenerate `current_status.md` | Antigravity | Next automation sprint |
| 8 | VPS sync: blue rebuild with API `789b266` + Web `4ad032a`, migrations `0010` + `axes`, Celery containers + SMTP env | Cursor / HitM | After PR #65 merge |
| 9 | Signals.py PII scrub (username in Loguru sinks) | Cursor | Add to T05 or T06 scope |
| 10 | Governance: admin artifacts restructure + decision matrix system | Claude Code | Future session |


