# Documentation & Governance Structure — Consolidation Proposal (D6 seed)

**Status:** ✅ RESOLVED 2026-06-29 — all sections approved, 5 open questions answered inline.
Decisions routed to permanent homes (see "Resolution" block below). Per
`governance/meeting_artifact_protocol.md`, this seed may be archived to `strategy/archived/` once
the Cursor restructure plan closes.

## Resolution — where each decision now lives (2026-06-29)

| Decision | Permanent home |
|---|---|
| Four root purposes + design_docs disposition | `PLAN_CHORE_DESIGN_DOCS_RESTRUCTURE_2026-06-29` (Cursor, submodule) |
| Q1 projections grouped | `strategy/projections/` (success_projection + quarterly_reviews moved in) |
| Q2 parking lot = multi-file | `strategy/parking_lot/` (seeded: morning-meeting-template, white-hat-ai-security, versioning-system-rework) |
| Q3 30_Releases pulled out; versioning deferred | `strategy/parking_lot/versioning-system-rework.md` + restructure plan T02 (Runtime_Signup_Sheet retained) |
| Audit issues home | `strategy/audits/` (operational_audit_report + improvement tracker moved in) |
| Upcoming/anticipated risks | `strategy/risk_register.md` |
| Q4 meeting-artifact lifecycle | `governance/meeting_artifact_protocol.md` |
| Q5 AGENTS.md additions (now) | `AGENTS.md` §1 — trust-but-verify + documentation-maintenance tenets |
| Index file | `strategy/README.md` (rebuilt as the living-state index) |

**Remaining (Cursor):** execute `PLAN_CHORE_DESIGN_DOCS_RESTRUCTURE_2026-06-29` (submodule moves +
Dashboard/Sync-Protocol fixes). **Downstream:** design-doc sweep automation prompt, against this
now-settled boundary.

---

_Original seed below (status was: SEED for a dedicated seated session — not for execution this session)._
**Generated by:** Claude Code (admin, 2026-06-29) · **Origin:** D6 in `strategy/meetings/admin_meeting_notes_2026-06-29.md`
**Builds on (does not replace):**

- `design_docs/10_Current_State/02_Documentation_Sync_Protocol.md` (existing boundary)
- `strategy/admin_overhaul_proposal_2026-06-26.md` (approved — guiding principles)
- `design_docs/10_Current_State/Strategic_doc_conflicts_pending_direction.md` (2026-05-01 audit trail)

> **How to use:** mark each item ✅ approve / ✏️ modify / ❌ reject. Nothing here is executed
> until you sign off in the seated session. This doc only *frames the boundary call* — it does
> not finalize structure, and it is **not** the design-doc sweep prompt (that is downstream of
> this decision).

---

## 0. Constraints carried in

1. `**design_docs/` is a separate git submodule** (own `.git`, Obsidian vault). Any move /
  retire / restructure is a cross-repo operation on the design_docs repo's own branch,
   **executed by Cursor** — consistent with the 2026-06-26 overhaul's "no submodule pointer
   changes from admin" line. This proposal authors the *plan*; it moves nothing.
2. **A boundary already exists** in the Documentation Sync Protocol. The problem is drift and
  under-enforcement, plus **no defined home** for several buckets you named. We refine, not
   reinvent.
3. **Worst-case resilience is a hard requirement (locked):** `design_docs/` must document *how
  each system actually works*, in enough depth that you could resume all work manually. That
   pins design_docs' purpose: **durable technical truth**, stripped of roadmap/release-ops
   material that belongs elsewhere and drifts.
4. **Keep agent reading-load low** (2026-06-26 principle: ≤3 files before a task). More homes
  is fine *only if* each has one obvious purpose.

---

## 1. Current state — what lives where (and the drift)


| Root                                 | Intended purpose                                                   | Drift / overlap observed                                                                                                                                                                                                                                                                            |
| ------------------------------------ | ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `design_docs/` (submodule, Obsidian) | Architecture + current state + roadmap-history + release ops       | 10/20/30/40 tiers overlap `strategy/` (roadmap) and `governance/` (release ops). `reflex_docs/` = archived product. `10_Current_State/00_Alpha_Overview` + `03_Resume_Checkpoint` = alpha-era. `00_Dashboard.md` references `30_Security/` and files (`Versioning_and_Contracts`) that don't exist. |
| `strategy/`                          | Living strategic state, research, meetings, anomalies, automations | Mixes structured subdirs (`meetings/`, `anomalies/`, `automations/`, `quarterly_reviews/`, `security/`) with loose one-off files (`operational_audit_report.md`, `audit_improvement_tracker_*`, screenshots, CBA docs). No home for "parked," "upcoming issues," "projections."                     |
| `governance/`                        | Plan ops: registry, lifecycle, templates, protocols                | Strong. Some protocol docs overlap `design_docs/30_Releases/` (git/workflow, deployment).                                                                                                                                                                                                           |
| `plans/`                             | Feature/chore plans + tasks                                        | Clean. Draft plans 55+ days old flagged but that's lifecycle, not structure.                                                                                                                                                                                                                        |


**One-line root boundary (proposed canonical):**

- `plans/` → **what we're going to build** (executable plans + tasks).
- `governance/` → **how we operate** (plan lifecycle, registry, protocols, templates — the rules).
- `strategy/` → **where we're going & how we're tracking it** (living state, decisions, meetings, research, audits, parked/upcoming).
- `design_docs/` → **how the built system actually works** (durable architecture + current technical state; the worst-case manual-resume reference).

- [x] Approve the four one-line root purposes  ✏️ modify: ____  ❌ reject

---

## 2. `design_docs/` 10/20/30/40 disposition (the question you raised)

Goal: make design_docs purely "how it works," so it stops stepping on `strategy/` and
`governance/`.


| Tier / folder                                                                                                                                                  | Proposed disposition                                                                                                                                                                                                                                            | Rationale                                                                    |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| `10_Current_State/`                                                                                                                                            | **KEEP, refocus** as true current technical state. Archive `00_Alpha_Overview`, `03_Resume_Checkpoint_2026-04-27`. Keep `02_Documentation_Sync_Protocol` (governing doc).                                                                                       | Alpha-era files are historical; the tier should describe what's *live now*.  |
| `20_Roadmap/`                                                                                                                                                  | **DEMOTE to historical archive** (canonical roadmap already = `strategy/strategic-roadmap-reframe`). Banner already says historical; finish the job — move active-sounding files under `_historical/`.                                                          | Roadmap truth is in strategy/. This tier is the #1 drift source.             |
| `30_Releases/`                                                                                                                                                 | **SPLIT:** git/workflow/deploy *rules* → canonical in `governance/` (already have `deployment_protocol.md`, `branching_guidelines.md`); keep `design_docs/30` copies as historical or retire. `**Runtime_Signup_Sheet.md` stays** but is reclassified (see §3). | Release *rules* are governance; design_docs shouldn't hold a competing copy. |
| `40_System_Design/`                                                                                                                                            | **KEEP** — this is the core "how it works" architecture tier.                                                                                                                                                                                                   | Exactly the worst-case-resilience material. Strengthen, don't move.          |
| `api_docs/` `cli_docs/` `rust_docs/`                                                                                                                           | **KEEP** — component deep-dives.                                                                                                                                                                                                                                | Durable technical truth.                                                     |
| `reflex_docs/`                                                                                                                                                 | **ARCHIVE** (move to `_historical/` or `.trash` per vault convention).                                                                                                                                                                                          | Archived product; web is flagship.                                           |
| loose top-level (`00_Coding_Guidelines`, `00_Encryption_Strategy`, `01_Business_Vision`, `02_Future_Risks`, `03_Localization_Strategy`, `50_Research_Backlog`) | **REVIEW individually** — most are durable specs (keep); `50_Research_Backlog` likely belongs to `strategy/research/`.                                                                                                                                          | Mixed; needs a one-pass triage in the session.                               |
| `00_Dashboard.md`                                                                                                                                              | **FIX** to match reality (remove dead `30_Security/` + nonexistent file links) once the above lands.                                                                                                                                                            | The index itself is drifted.                                                 |


- [x] Approve dispositions as a starting frame  ✏️ modify rows: ____  ❌ reject

---

## 3. Homes for the buckets you named (D6 comment)

You listed: daily meetings, long-term projection discussions, postponed items, upcoming
issues, audit issues. Proposed homes (all under `strategy/` — living state):


| Bucket                                          | Recommended home                                        | Notes / status                                                                                                                                           |
| ----------------------------------------------- | ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Daily meetings                                  | `strategy/meetings/` ✅ already exists                   | Formalize with the D8 template once D5/D6 settle.                                                                                                        |
| Long-term projection discussions                | **NEW `strategy/projections/`**                         | Absorbs `success_projection_2026-06.md`; future-band / multi-quarter thinking. Distinct from quarterly_reviews (retrospective) vs projections (forward). |
| Postponed / parked items                        | **NEW `strategy/parking_lot.md`** (single rolling file) | Today "parked" items scatter across meeting notes + draft plans. One canonical list with a date + "revisit-when" trigger per item.                       |
| Upcoming issues (anticipated, not yet occurred) | **NEW `strategy/risk_register.md`**                     | Anomalies = *detected*; risk register = *anticipated*. Keeps the two from blurring.                                                                      |
| Audit issues                                    | **NEW `strategy/audits/*`*                              | Absorbs `operational_audit_report.md`, `audit_improvement_tracker_2026-06-27.md`, and ties to `strategy/security/` audit reports.                        |


- [x] Approve homes  ✏️ modify (e.g. fold risk_register into parking_lot, or audits into governance): ____  ❌ reject

---

## 4. Open questions for the seated session

1. **Projections vs quarterly_reviews vs success_projection** — one forward-looking home, or keep separate cadences?  These are interconnected docs, with different individual purposes.  These should probably be grouped together since each relatively affects teh other
2. **Parking lot granularity** — single file, or a dir with one file per parked item (like anomalies)? this will likely be multi files, since each parked item might be iterated upon multiple times, so maintaining context per issue keeps things cleans, even if it introduces a bit of extra clutter.  
3. `**30_Releases` rules** — fully migrate to `governance/` and retire the design_docs copies, or keep design_docs as the "deep version" and governance as the "quickstart"?  We can probably pull this out entirely, as it was suppsoed to be a summary of each version, but our versioning system is broken as it stands, as we're doing multiple small fixes isntead of large changes.  worth revisting after finishing all S1.B features except pay methods as a potential, but likely unnessary.  
4. `**strategy/` loose-file cleanup** — screenshots, CBA docs, one-off proposals: archive into `strategy/archived/` (already exists) on a cadence? Yes, this should be archived at a certain point.  some of these are just there for the meeting in the moment.  We should have some protocols on what files get saved, updated, or archived within meetings.  
5. **AGENTS.md additions** (deferred, your call): trust-but-verify tenant + documentation-maintenance notes (keep `Runtime_Signup_Sheet` / VPS checkout sheet current). Bundle into this session or its own.  we should bundle this into this meeting, before I send cursor to start its tasks for the day. 

---

## 5. What executes *after* approval (not now)

- **Cursor** (design_docs submodule branch, e.g. `cur/s1b/chore/design-docs-restructure`): the moves/archives/retires + `00_Dashboard.md` fix.
- **Claude Code**: create the new `strategy/` homes (projections/, parking_lot, risk_register, audits/), update the Documentation Sync Protocol to match the new boundary, AGENTS.md additions.
- **Then** (downstream, separate): author the design-doc sweep automation prompt against the *settled* boundary — this is the original D6 automation ask, and it was correctly blocked on this decision.

- [x] Approve the execution split  ✏️ modify: ____  ❌ reject