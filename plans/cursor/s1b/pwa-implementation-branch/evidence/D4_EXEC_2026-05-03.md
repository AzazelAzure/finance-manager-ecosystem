# D4-exec evidence — 2026-05-03

**Environment:** `dev@159.198.75.194` (`~/finance_manager`), proxy **:8443**, **active color: blue** (from `./scripts/fm_server_beta.sh status`).

## §2 — Operator / automated

| Check | Result | Notes |
| ----- | ------ | ----- |
| `./scripts/fm_server_beta.sh status` | **PASS** | Active blue, inactive green; `fm-beta_proxy_1` up on `0.0.0.0:8443->443/tcp`. |
| `./scripts/fm_server_beta.sh smoke --color active` | **PASS** | Redis PING, `api-blue` `/api/health/`, `web-blue` internal `wget` root, Host-header curls via `FM_PUBLIC_BASE_URL=https://127.0.0.1:8443` with `jsdevtesting` / `api-jsdevtesting` host headers. |

## §3–§4 — Chrome desktop + Chrome Android (certified)

| Section | Result | Notes |
| ------- | ------ | ----- |
| D4 §3.1–§3.6 (Chrome desktop) | **NOT RUN** | Requires interactive DevTools (manifest, SW, offline shell, outbox, logout confirm, post-switch installed PWA) against the promoted web image on the public hostname. |
| D4 §4 (Chrome Android) | **NOT RUN** | Same as above on device or emulator. |

**Signer (automated subset):** agent session 2026-05-03. **HitM sign-off** still required on the checklist rows above before claiming full **D4-exec** closure per research README.

## Follow-up

After the PWA-capable **web** image is deployed to the target color, run D4 §3–§4 on **`https://<public-host>:8443`** and append a second table here or replace this file’s §3–§4 section with dated PASS/FAIL initials.
