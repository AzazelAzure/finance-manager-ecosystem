# T03 execution notes — 2026-04-29 (orchestrator, host session)

## Attempt

- Host: `dev@159.198.75.194`  
- Path: `/home/dev/finance_manager`  
- Env: `FM_PUBLIC_APP_HOST=thehivemanager.com`, `FM_PUBLIC_API_HOST=api.thehivemanager.com`, `FM_PUBLIC_BASE_URL=https://127.0.0.1:8443`  
- Command: `./scripts/fm_server_beta.sh deploy green` (after `--dry-run` showed `up -d db api-green reflex-green proxy`)

## Outcome: **failed / rolled back** (no cutover, no switch)

### Root cause (primary)

`docker-compose.bluegreen.yml` maps **host** ports `5432`, `8080`, `8443` for the `fm-beta` project. The **single-stack** `finance_manager` project already uses **the same** host ports (`finance-manager-db` on 5432, `finance-manager-proxy` on 8080/8443).  
`podman` error: `rootlessport listen tcp 0.0.0.0:5432: bind: address already in use`

**Conclusion:** Inactive-color deploy **cannot** succeed on this host **while** the legacy stack is up. T03 as written assumes a free host or a **migration cut** from single-stack → `fm-beta` (maintenance window), not side-by-side operation.

### Secondary issues observed (after port failure)

- `api-blue` image not present; compose uses short image names in some code paths — further investigation if/when `fm-beta` is started on a clean host.
- Script still printed `Deployed candidate color: green` despite partial failure — treat exit logs and `podman ps` as source of truth.

## Cleanup (completed)

- `podman-compose -p fm-beta -f docker-compose.bluegreen.yml down -v` and removal of `fm-beta_*` containers; volumes `fm-beta_postgres_data`, `fm-beta_redis_data` removed.
- **Verified:** `finance-manager-{db,api,reflex,proxy}` still **Up** and api **healthy** after cleanup.

## What would unblock T03

1. **Maintenance window:** stop single-stack (`fm_docker.sh stop` or equivalent), then bring up full `fm-beta` (or `deploy` both colors per script design) and re-point tunnel — **data/DB strategy** must be defined (same Postgres data dir vs import).
2. **Or** change `docker-compose.bluegreen.yml` / deploy flow so the candidate stack does **not** publish conflicting host ports and reuses the running DB (larger design change; not done here).

## Smoke

- `./scripts/fm_server_beta.sh smoke --color inactive` was **not** run; deploy did not complete.
