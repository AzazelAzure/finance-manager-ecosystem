# Archived: Gemini-named plan templates (superseded)

These three files (`GEMINI_PLAN_TEMPLATE*.md`) lived under **`plans/templates/`** as alternate plan shapes for parallel assistants.

**Superseded (2026-05-04):** All governed execution plans use a **single** schema — **`governance/plan_template.md`** — for every agent (Cursor, Gemini, etc.). Copy its YAML header and directory layout; register in **`governance/plan_registry.md`**.

Do not revive these paths as active templates; keep them here for historical diff reference only.
