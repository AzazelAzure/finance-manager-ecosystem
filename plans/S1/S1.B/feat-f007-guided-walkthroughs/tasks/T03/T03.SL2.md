# T03.SL2: Dashboard Auto-Start Policy

## Scope
- **Surface**: Web Dashboard (`DashboardPage.tsx`)
- **Focus**: Linear tour auto-start logic.

## Execution Checklist
- [ ] Ensure that the dashboard tour is automatically triggered for new users who do not have `dashboard_linear_tour` in their `completed_tours` list.
- [ ] Add a small delay (e.g., `setTimeout` for 1000ms) after data finishes loading before triggering `startTour` to ensure the DOM is painted and targets are available.

## Verification
- Clear `completed_tours` for the active profile. Reload the Dashboard. The tour should start automatically.
- Refresh the page *after* completing the tour. The tour must not start again.
