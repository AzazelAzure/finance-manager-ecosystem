# T03.SL1: Dashboard DOM Targets

## Scope
- **Surface**: Web Dashboard (`DashboardPage.tsx`)
- **Focus**: DOM stabilization for the linear tour.

## Execution Checklist
- [x] Wire `dashboard_linear_tour` with 8 steps (KPIs, Filters, Flow, Spend, Category, Tag, Balances, Snapshot).
- [x] Ensure DOM targets like `#tour-flow-chart`, `#tour-category-chart` exist.
- [x] Provide detailed widget-level tutorials explaining the "why" and "how" of each dashboard component.

## Verification
- Inspect the Dashboard HTML to ensure IDs match the `dashboard_linear_tour` steps payload.
- Ensure no "target not found" console errors are thrown when the tour executes.
