# T01.SL1: API Migration & Persistence Behavior

## Scope
- **Surface**: API only
- **Focus**: Verify the `AppProfile.completed_tours` field exists and correctly persists data.

## Execution Checklist
- [ ] Verify `0007_appprofile_completed_tours.py` migration is applied to the database.
- [ ] Verify `GET /finance/profile/` returns a `completed_tours` list in the response.
- [ ] Verify `PATCH /finance/profile/` handles `completed_tours` idempotently (i.e. replacing the entire list with the client's payload rather than union-appending blindly without tracking state).

## Verification
- Test using Pytest (`test_profile_patch.py`) or manual PATCH requests.
- Ensure the state accurately reflects the exact list of tours submitted by the client.
