# T01.SL2: API Contract Documentation

## Scope
- **Surface**: API Contract / Web Payload
- **Focus**: Documenting the list-merge behavior.

## Execution Checklist
- [x] Add docstring documentation to `finance_manager_api/finance/services/user_services.py` specifying that `completed_tours` requires a **replace-merge** behavior (the client must send the entire resulting list).
- [x] Align the web `updateAppProfile` payload generation in `finance_manager_web/src/api/profile.ts` so that it passes the full merged array to the backend.

## Verification
- [x] Document the replace-merge list behavior in `user_services.py` and `profile_serializers.py`.
- [x] Ensure `TourProvider` aligns with this behavior by sending the full unioned list.
