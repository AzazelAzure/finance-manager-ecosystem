# T04.SL1: Transactions Linear Tour

## Scope
- **Surface**: Web Transactions (`TransactionsPage.tsx`)
- **Focus**: DOM stabilization for the transactions linear tour.

## Execution Checklist
- [ ] Verify the existence of the expected DOM IDs (e.g., `#tx-filters`, `#tx-add`, `#tx-table`).
- [ ] Add `startTour('transactions_linear_tour', [...])` inside a `useEffect` triggered on page load.
- [ ] Verify completion ID stability (`transactions_linear_tour` is correctly saved to the profile).

## Verification
- Inspect the Transactions page HTML to ensure IDs match the steps payload.
- Tour should trigger automatically if the user has not completed it.
