# T02.SL1: TourProvider Joyride Execution

## Scope
- **Surface**: Web `TourProvider`
- **Focus**: Joyride modal lifecycle.

## Execution Checklist
- [x] Integrate `react-joyride` into `TourProvider.tsx`.
- [x] Verify Joyride runs and shows tooltips.
- [x] Verify that linear tours (e.g., `dashboard_linear_tour`) do not auto-start if already in `completed_tours`.
- [x] Verify that skipping or finishing the tour correctly calls `setRun(false)` and clears the active tour state.
- [ ] Ensure that `TourProvider` does **not** contain business logic to auto-start tours on specific pages (auto-starts belong in page components).

## Verification
- Trigger a dummy tour manually (or via Storybook/minimal route) and confirm the component lifecycle does not crash or loop.
