# T02.SL3: Help Mode vs Joyride UX

## Scope
- **Surface**: Web `HelpModeWrapper` and `TourProvider`
- **Focus**: Aligning Help Mode with the decided UX strategy.

## Execution Checklist
- [x] Implement the Hybrid approach: Help Mode triggers single-step joyride notes for the highlighted widgets rather than a custom tooltipping engine.
- [x] Refine `HelpModeWrapper` to use specific `id` targets for contextual Joyride tooltips.
- [x] Add basic keyboard support (Enter/Space) and ARIA labels for accessibility.
- [x] Ensure `disableBeacon: true` for contextual help to avoid cluttering the UI.
- [x] Ensure the pulse/highlight effects are active when Help Mode is enabled via the global nav.

## Verification
- Toggle "Help Mode" in the navigation shell.
- Click a highlighted `HelpModeWrapper` (e.g. on Dashboard) and verify a Joyride beacon/tooltip appears contextual to that element.

## Status
- [x] PASS
