# T02.SL3: Help Mode vs Joyride UX

## Scope
- **Surface**: Web `HelpModeWrapper` and `TourProvider`
- **Focus**: Aligning Help Mode with the decided UX strategy.

## Execution Checklist
- [ ] Implement the Hybrid approach: Help Mode triggers single-step joyride notes for the highlighted widgets rather than a custom tooltipping engine.
- [ ] Remove redundant/duplicate logic paths if Help Mode and Joyride were previously implemented via two separate overlay libraries.
- [ ] Ensure the pulse/highlight effects are active when Help Mode is enabled via the global nav.

## Verification
- Toggle "Help Mode" in the navigation shell.
- Click a highlighted `HelpModeWrapper` (e.g. on Dashboard) and verify a Joyride beacon/tooltip appears contextual to that element.
