# T02.SL2: TourProvider Persistence Wire

## Status: PASS

## Scope
- **Surface**: Web `TourProvider`
- **Focus**: Connecting tour completion to the AppProfile API.

## Execution Checklist
- [x] Wire `markTourCompleted` to `handleJoyrideCallback` for `FINISHED` or `SKIPPED` statuses.
- [x] Ensure `completed_tours` list is updated correctly via `mutate`.
- [x] Exclude `help_` prefixed tours from persistence.
- [x] Ensure `markTourCompleted` calls `updateProfile({ completed_tours: [...completedTours, tourId] })`.
- [x] Ensure that upon success, the `['app-profile']` react-query cache is invalidated to refetch the new state.

## Verification
- Observe network tabs during a tour finish event. The PATCH request should contain the updated array.
- Subsequent calls to `isTourCompleted(tourId)` should return true.
