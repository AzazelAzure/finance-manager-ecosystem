services:
  db:
    image: docker.io/library/postgres:15-alpine
    environment:
      POSTGRES_DB: ${DB_NAME:-finance_db}
      POSTGRES_USER: ${DB_USER:-finance_user}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-finance_password}
    ports:
      - "5432:5432"
    volumes:
      # Reuse live single-stack data on cutover: name must match `podman volume ls` (e.g. finance_manager_postgres_data)
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: docker.io/library/redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    expose:
      - "6379"
    volumes:
      - redis_data:/data

  api-blue:
    build:
      context: ./finance_manager_api
      dockerfile: Dockerfile
    environment:
      - DB_ENGINE=django.db.backends.postgresql
      - DB_NAME=${DB_NAME:-finance_db}
      - DB_USER=${DB_USER:-finance_user}
      - DB_PASSWORD=${DB_PASSWORD:-finance_password}
      - DB_HOST=db
      - DB_PORT=5432
      - SECRET_KEY=${SECRET_KEY}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:-api.financemanager.local,api-blue,api-green,api.thehivemanager.com,localhost,127.0.0.1}
      - DEBUG=${DEBUG:-False}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379/0}
      - BUG_REPORT_TO_EMAIL=${BUG_REPORT_TO_EMAIL:-}
      - SUPPORT_DIGEST_TO_EMAIL=${SUPPORT_DIGEST_TO_EMAIL:-}
      - OPERATOR_NOTIFY_EMAIL=${OPERATOR_NOTIFY_EMAIL:-}
      - BETA_FEATURE_REQUESTS_ENABLED=${BETA_FEATURE_REQUESTS_ENABLED:-False}
      - EMAIL_BACKEND=${EMAIL_BACKEND:-django.core.mail.backends.smtp.EmailBackend}
      - EMAIL_HOST=${EMAIL_HOST:-}
      - EMAIL_PORT=${EMAIL_PORT:-587}
      - EMAIL_USE_TLS=${EMAIL_USE_TLS:-True}
      - EMAIL_HOST_USER=${EMAIL_HOST_USER:-}
      - EMAIL_HOST_PASSWORD=${EMAIL_HOST_PASSWORD:-}
    depends_on:
      - db
      - redis
    expose:
      - "8000"
    volumes:
      - logs:/app/logs
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health/"]
      interval: 30s
      timeout: 5s
      retries: 5

  celery-worker-blue:
    build:
      context: ./finance_manager_api
      dockerfile: Dockerfile
    command: uv run celery -A finance_api worker -l info
    environment:
      - DB_ENGINE=django.db.backends.postgresql
      - DB_NAME=${DB_NAME:-finance_db}
      - DB_USER=${DB_USER:-finance_user}
      - DB_PASSWORD=${DB_PASSWORD:-finance_password}
      - DB_HOST=db
      - DB_PORT=5432
      - SECRET_KEY=${SECRET_KEY}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:-api.financemanager.local,api-blue,api-green,api.thehivemanager.com,localhost,127.0.0.1}
      - DEBUG=${DEBUG:-False}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379/0}
    depends_on:
      - db
      - redis
    volumes:
      - logs:/app/logs

  celery-beat:
    build:
      context: ./finance_manager_api
      dockerfile: Dockerfile
    command: uv run celery -A finance_api beat -l info
    environment:
      - DB_ENGINE=django.db.backends.postgresql
      - DB_NAME=${DB_NAME:-finance_db}
      - DB_USER=${DB_USER:-finance_user}
      - DB_PASSWORD=${DB_PASSWORD:-finance_password}
      - DB_HOST=db
      - DB_PORT=5432
      - SECRET_KEY=${SECRET_KEY}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:-api.financemanager.local,api-blue,api-green,api.thehivemanager.com,localhost,127.0.0.1}
      - DEBUG=${DEBUG:-False}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379/0}
      - BUG_REPORT_TO_EMAIL=${BUG_REPORT_TO_EMAIL:-}
      - SUPPORT_DIGEST_TO_EMAIL=${SUPPORT_DIGEST_TO_EMAIL:-}
      - OPERATOR_NOTIFY_EMAIL=${OPERATOR_NOTIFY_EMAIL:-}
      - BETA_FEATURE_REQUESTS_ENABLED=${BETA_FEATURE_REQUESTS_ENABLED:-False}
      - EMAIL_BACKEND=${EMAIL_BACKEND:-django.core.mail.backends.smtp.EmailBackend}
      - EMAIL_HOST=${EMAIL_HOST:-}
      - EMAIL_PORT=${EMAIL_PORT:-587}
      - EMAIL_USE_TLS=${EMAIL_USE_TLS:-True}
      - EMAIL_HOST_USER=${EMAIL_HOST_USER:-}
      - EMAIL_HOST_PASSWORD=${EMAIL_HOST_PASSWORD:-}
    depends_on:
      - db
      - redis
    volumes:
      - logs:/app/logs

  api-green:
    build:
      context: ./finance_manager_api
      dockerfile: Dockerfile
    environment:
      - DB_ENGINE=django.db.backends.postgresql
      - DB_NAME=${DB_NAME:-finance_db}
      - DB_USER=${DB_USER:-finance_user}
      - DB_PASSWORD=${DB_PASSWORD:-finance_password}
      - DB_HOST=db
      - DB_PORT=5432
      - SECRET_KEY=${SECRET_KEY}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:-api.financemanager.local,api-blue,api-green,api.thehivemanager.com,localhost,127.0.0.1}
      - DEBUG=${DEBUG:-False}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379/0}
    depends_on:
      - db
      - redis
    expose:
      - "8000"
    volumes:
      - logs:/app/logs
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health/"]
      interval: 30s
      timeout: 5s
      retries: 5

  celery-worker-green:
    build:
      context: ./finance_manager_api
      dockerfile: Dockerfile
    command: uv run celery -A finance_api worker -l info
    environment:
      - DB_ENGINE=django.db.backends.postgresql
      - DB_NAME=${DB_NAME:-finance_db}
      - DB_USER=${DB_USER:-finance_user}
      - DB_PASSWORD=${DB_PASSWORD:-finance_password}
      - DB_HOST=db
      - DB_PORT=5432
      - SECRET_KEY=${SECRET_KEY}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:-api.financemanager.local,api-blue,api-green,api.thehivemanager.com,localhost,127.0.0.1}
      - DEBUG=${DEBUG:-False}
      - REDIS_URL=${REDIS_URL:-redis://redis:6379/0}
    depends_on:
      - db
      - redis
    volumes:
      - logs:/app/logs

  api-green:
  web-blue:
    build:
      context: ./finance_manager_web
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://api.thehivemanager.com}
        VITE_STAGING_API_BASE_URL: ${VITE_STAGING_API_BASE_URL:-https://api-jsdevtesting.thehivemanager.com}
    expose:
      - "80"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  web-green:
    build:
      context: ./finance_manager_web
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://api.thehivemanager.com}
        VITE_STAGING_API_BASE_URL: ${VITE_STAGING_API_BASE_URL:-https://api-jsdevtesting.thehivemanager.com}
    expose:
      - "80"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  proxy:
    build:
      context: ./proxy
    ports:
      - "8080:80"
      - "8443:443"
    volumes:
      - ./proxy/certs:/etc/nginx/certs:ro,z
      - ./proxy/nginx.bluegreen.conf:/etc/nginx/nginx.conf:ro,z
      - ./proxy/active_color.conf:/etc/nginx/conf.d/active_color.conf:ro,z
    depends_on:
      - api-blue
      - api-green
      - web-blue
      - web-green
      - redis
    stop_signal: SIGQUIT
    stop_grace_period: 30s

volumes:
  # Reuse the same host volume across restarts; create once if missing: `podman volume create finance_manager_postgres_data`
  postgres_data:
    name: finance_manager_postgres_data
    external: true
  redis_data:
  logs:
